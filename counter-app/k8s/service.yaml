apiVersion: v1
kind: Service
metadata:
  name: counter-app
  namespace: counter
spec:
  type: NodePort
  selector:
    app: counter-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30501
